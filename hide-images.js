(function () { 
    Array.from(document.querySelectorAll('img')).forEach(item => { item.style.display = 'none' }); 
    Array.from(document.querySelectorAll('video')).forEach(item => { item.style.display = 'none' });
})();